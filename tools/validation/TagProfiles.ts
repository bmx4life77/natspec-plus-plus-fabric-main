export const TagProfiles: Record<string, string[]> = {
  executeParallel: ["parallel", "threadSafe", "maxConcurrency", "gasOptimized"],
  emergencyWithdraw: ["reentrancyGuard", "accessControlled", "auditCritical"],
  // Add more methods as your protocol grows
};
